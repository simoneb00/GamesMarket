package GamesMarket.control;


import GamesMarket.bean.PostBean;
import GamesMarket.graphicControl.forum.PostGraphicController;
import GamesMarket.model.Post;

public class ForumController {
/*
    public void postButton(PostBean postBean) {
        String text = postBean.getText();
        String username = postBean.getUsername();

        Post post = new Post();
        post.setUsername(username);
        post.setText(text);

        this.addPost(post);
    }


    public void addPost(Post post) {
        PostGraphicController pgc = new PostGraphicController();
        PostBean postBean = new PostBean();
        postBean.setUsername(post.getUsername());
        postBean.setText(post.getText());
        pgc.setNewData(postBean);
    }

     */
}
